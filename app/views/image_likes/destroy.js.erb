template = "<%=j button_to "Like Image", like_image_path(@image), remote: true %>";
$("#image-like-button").html(template);

template = "<%=j pluralize(@image.likes.count, "like") %>";
$("#count").html(template);