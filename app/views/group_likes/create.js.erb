template = "<%=j button_to "Unlike Group", unlike_group_path(@group), remote: true, method: :delete %>";
$("#like-button").html(template);

count = "<%= pluralize(@group.likes.count, "Like") %>";
$("#count").html(count);