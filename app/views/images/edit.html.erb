<%= form_for(@image) do |form| %>
  <% render "form_errors", target: @image %>
  <%= form.text_field :name %>
  <%= form.collection_check_boxes :group_ids, @groups.all, :id, :name %>
  <%= form.submit "Submit" %>
<% end %>